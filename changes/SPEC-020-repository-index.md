# Change Request — SPEC-020

## Status
[x] Complete

## Assigned Model Tier
[x] Tier 2 — Local execution agent (Qwen3.8B)
    Permitted modes: IMPLEMENT only

## Objective
Extend `scripts/repository-all.ps1` to generate `repository-index.md` — path, version, status, canonical flag, and architectural layer for every `_ORVION_CANONICAL/*.md` document, derived entirely from existing headers, never hand-maintained.

## Business Reason
This is the reduced Repository Index already validated: every field is mechanically derivable from headers already present, extending the same script that already reliably regenerates `project-tree.txt`/`git-tree.txt`/`tracked-files.txt`. Scoped to index generation only — the broken-reference/staleness health check discussed alongside it is real, separate scripting work deserving its own careful design, not bundled into continuous-execution momentum.

## Risks
None. Read-only script logic against existing files; output is a new generated file, never hand-edited per this repository's own convention for generated artifacts.

## Supersedes / Depends On
Supersedes: None. Depends on: None technically, though output is most meaningful once SPEC-012/013/018 (Complete) have normalized the headers it reads.

## Scope — Files Allowed to Modify
- scripts/repository-all.ps1

## Out of Scope — Files Forbidden to Modify
Every existing file except the script. `repository-index.md` is a new generated output, not hand-authored content — it is created by running the script, not by this Change Request's own Implementation Steps directly.

## Minimum Reading List
- scripts/repository-all.ps1

## Implementation Steps

### Step 1 — Add Repository Index generation to repository-all.ps1
Verify: search `scripts/repository-all.ps1` for the exact string `Refresh Repository Index`. If found: Already Applied, skip. If not found: insert, immediately after the existing "[1/7] Refresh Project Tree" block and before "[2/7] Refresh Git Files", a new step that: lists every `*.md` file under `_ORVION_CANONICAL/`; for each, extracts `Version:`, `Status:`, and `Canonical:` from its header via regex; derives Layer from the file's leading number (`00`-`23` = business/domain, `24`-`33` = schema/database); writes the result as a Markdown table to `repository-index.md` at repository root, headed "Generated by scripts/repository-all.ps1 — do not hand-edit." Renumber the script's subsequent step labels (`[2/7]` through `[7/7]`) to `[2/8]` through `[8/8]` accordingly.

## Acceptance Criteria
- [x] The script contains the new Index-generation step, correctly positioned.
- [x] Step labels are renumbered consistently, `[1/8]` through `[8/8]`.
- [x] No other part of the script's logic (tree/git-file refresh, commit, push) is altered.
- [x] No file outside Scope was modified.

## Execution Log

### 2026-07-02 20:50 — Claude (Sonnet 5)

Outcome: Complete

Step results:
- Step 1: Applied — Repository Index generation step inserted as [2/8], between Tree refresh [1/8] and Git Files refresh [3/8]; all subsequent step labels renumbered [3/8] through [8/8] consistently.

Commits: pending — recorded at commit time in the same commit as this entry.

Verification performed before this entry: `git status --porcelain` confirmed exactly the one Scope file changed; `repository-index.md` correctly not created by this Change Request itself, since it is generated output produced only when the script is run.

### 2026-07-02 — Complete (human command)

Phase-freeze check: not scoped to any roadmap phase — Repository Engineering work. Not applicable.

## Verification Notes

### 2026-07-02 20:51 — Claude (Sonnet 5)

Verdict: Confirmed Complete

Findings: `git diff 6ceb381 HEAD` on scripts/repository-all.ps1 matches the Implementation Step exactly — Index-generation block correctly positioned as [2/8], all subsequent labels renumbered consistently, no other logic altered. Additionally parsed the file with PowerShell's own language parser (`[System.Management.Automation.Language.Parser]::ParseFile`, no execution) to confirm the inserted block introduced no syntax error — result: no parse errors. No file outside Scope touched.

Recommendation to human: Set Status to Complete

## Review Gate
- [x] Every change matches the Implementation Steps exactly, or was correctly recorded as Already Applied.
- [x] No file outside the Scope list was modified or created.
- [x] Every Acceptance Criteria item is confirmed true.
- [x] The repository is in a clean, releasable state.

## Notes
Explicitly not blocking SQL Readiness — Packages 1–5 are the gate, this is Package 6, correctly non-blocking. The broken-reference/staleness Health check remains a distinct, deliberately deferred follow-on, not silently dropped.
